What is the difference between var, let, and const?
ans: var is old way to declare, it is like function scoped and can be redeclared,
and let is modern way , it is block scope and it can be reassign, and lastly const is a block scope but it can not be reassigned>

What is the spread operator (...)?
ans: spread operator is used to make expand or copy the array or object .

difference between map(), filter(), and forEach()?
ans: first , map create a new array through changing every time , secondly filter() : filter() create new array with only matching item, lastly : forEach() it is loop like it runs code for each item , but it does not return any new array .

What is an array function () ?
ans: array function is a shorter version of function in JavaScript.

What are template literals ?
ans: template literals is modern way to write string using backtick inside template literals we can set value dynamic and it allow to write multi line strings and it allow to insert value like `${value}`
